[pytest]
# 配置参数，‘-vs’:详细 + 显示print
addopts = -v   -m "smoke or user"
# ‘-m’:指定执行哪个标记用例，如果想执行多个，可用‘or’链接

# 改变用例的查找规则
testpaths = ./testcases

# 改变模块的查找规则
python_files = test_*.py

# 改变类的查找规则
python_classes = Test*

#改变函数的查找规则
python_functions = test_*

# 标记
markers =
    smoke:冒烟测试
    user:用户管理